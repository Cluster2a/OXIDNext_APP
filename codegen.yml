overwrite: true
schema: 'http://localhost:8080/graphql/'
generates:
  src/lib/generated/graphql.ts: # Typescript types (output generated file)
    plugins: # List of needed plugins (installed as devDeps)
      - typescript
      - typescript-operations
